true = {
    if = whenTrue whenFalse > {
        ...whenTrue;
    };
};
false = {
    if = whenTrue whenFalse > {
        ...whenFalse;
    };
};

person = height length > {
    name = "John";
    age = name;
    l = 5;
};

main = print(5);
